// ============================================================
//  前端配置 — 复制此文件为 config.js 并替换占位符
//  Frontend config — copy to config.js and replace placeholders
// ============================================================

// 后端 API 基础地址（不含 /api 后缀）
const API_BASE_URL = 'https://YOUR_DOMAIN';

// 后端 API 地址（含 /api 后缀，供页面直接使用）
const API_BASE = 'https://YOUR_DOMAIN/api';

// 静态资源基础地址（图片等）
const IMG_BASE = 'https://YOUR_DOMAIN';

module.exports = { API_BASE_URL, API_BASE, IMG_BASE };